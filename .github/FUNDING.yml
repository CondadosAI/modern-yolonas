# GitHub reads this file from the default branch only (main), so the Sponsor
# button appears after the next dev -> main merge, not before.
#
# Sponsorship pays for GPU time: the goal is COCO weights trained from scratch
# and published under Apache-2.0, so the project stops depending on Deci's
# non-commercial checkpoints.
github: [Gabriellgpc]
